<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Valentine cho chị 💖</title>

<style>
body {
    margin: 0;
    font-family: 'Segoe UI', sans-serif;
    background: linear-gradient(135deg, #ff758c, #ff7eb3);
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    overflow: hidden;
}

.box {
    background: white;
    border-radius: 20px;
    padding: 25px;
    max-width: 360px;
    text-align: center;
    box-shadow: 0 10px 25px rgba(0,0,0,0.25);
    animation: float 3s ease-in-out infinite;
}

h1 {
    color: #ff4d6d;
    font-size: 22px;
}

p {
    font-size: 16px;
}

button {
    margin-top: 15px;
    padding: 10px 18px;
    border: none;
    border-radius: 12px;
    background: #ff4d6d;
    color: white;
    font-size: 16px;
    cursor: pointer;
    transition: 0.2s;
}

button:hover {
    background: #ff1f4a;
    transform: scale(1.05);
}

.heart {
    position: fixed;
    color: red;
    font-size: 20px;
    animation: fall linear forwards;
}

@keyframes fall {
    from { transform: translateY(-10vh); opacity: 1; }
    to { transform: translateY(110vh); opacity: 0; }
}

@keyframes float {
    0%,100% { transform: translateY(0); }
    50% { transform: translateY(-8px); }
}
</style>
</head>

<body>

<div class="box">
    <h1 id="title">💘 Valentine 💘</h1>
    <p id="text">Chị ơi, bấm để mở lời nhắn nha 💌</p>
    <button onclick="nextStep()">Mở 💝</button>
</div>

<script>
const name = "Tên chị"; // 👉 Đổi tên chị tại đây
let step = 0;

function nextStep() {
    const title = document.getElementById("title");
    const text = document.getElementById("text");

    if (step === 0) {
        title.innerHTML = "💖 Gửi đến " + name;
        text.innerHTML = "Chúc chị Valentine thật vui và ấm áp 🥰";
    }

    if (step === 1) {
        title.innerHTML = "💌 Lời nhắn riêng";
        text.innerHTML = 
        "Chị là người rất đặc biệt ✨<br>" +
        "Mong chị luôn xinh đẹp, hạnh phúc và được yêu thương thật nhiều 💞";
    }

    if (step === 2) {
        title.innerHTML = "🎮 Mini Game";
        text.innerHTML = 
        "Chị có muốn nhận thêm quà không? 😏<br><br>" +
        "<button onclick='win()'>Có 💕</button> " +
        "<button onclick='troll()'>Không 😆</button>";
    }

    step++;
}

function win() {
    document.getElementById("title").innerHTML = "💝 Phần thưởng";
    document.getElementById("text").innerHTML =
    "Chúc chị luôn rạng rỡ, vui vẻ và được trân trọng mỗi ngày 💘<br><br>" +
    "— Từ người luôn quan tâm chị 💌";
}

function troll() {
    document.getElementById("title").innerHTML = "😝 Dù chị chọn gì";
    document.getElementById("text").innerHTML =
    "Thì chị vẫn là người dễ thương nhất 💗";
}

// Tim rơi
setInterval(() => {
    const heart = document.createElement("div");
    heart.classList.add("heart");
    heart.innerHTML = "❤️";
    heart.style.left = Math.random() * 100 + "vw";
    heart.style.animationDuration = Math.random() * 2 + 3 + "s";
    document.body.appendChild(heart);

    setTimeout(() => heart.remove(), 5000);
}, 160);
</script>

</body>
</html>
